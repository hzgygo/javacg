package ${relativeProjectPath}.${entityPath}.base;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import ${path_core}.controller.BaseController;

public class Base${entity}Controller extends BaseController{

	private static Logger logger = LoggerFactory.getLogger(Base${entity}Controller.class);


}
